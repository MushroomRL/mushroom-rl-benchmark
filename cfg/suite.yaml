%YAML 1.2
---
suite_params:
  log_dir: logs
  log_id: benchmark
  use_timestamp: true
  parallel:
    max_concurrent_runs: 5
    threading: false
  slurm:
    hours: 24
    minutes: 0
    aggregate_hours: 3
    aggregate_minutes: 0
    only_print: false
    aggregation_job: true
    memory: 8000
    # gres: 'gpu:rtx2080:1'
    partition: test24
